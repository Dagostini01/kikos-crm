# Em desenvolvimento, o Vite faz proxy de /api → http://localhost:3333
VITE_API_URL=/api

# Em produção (Vercel), use a URL pública da API (sem barra no final), por exemplo:
# VITE_API_URL=https://kikos-crm-api.onrender.com
