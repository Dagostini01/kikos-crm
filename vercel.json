{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @kikos/web build",
  "outputDirectory": "apps/web/dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
